# Run with `dart run ffigen --config ffigen.yaml`.
name: CribcallQuicBindings
description: |
  Bindings for `src/cribcall_quic.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/cribcall_quic_bindings_generated.dart'
headers:
  entry-points:
    - 'src/cribcall_quic.h'
  include-directives:
    - 'src/cribcall_quic.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
