#include "cribcall_quic.h"

// Rust + Cargokit build the actual QUIC library; this file remains as a stub
// so legacy CMake scaffolding stays satisfied for platforms that still scan
// the src directory.
